# `canari_ml predict` CLI Help

Run the following command to get the help information for `canari_ml predict` command:

``` console exec="on" source="material-block" result="ansi"
$ canari_ml predict --help
```
