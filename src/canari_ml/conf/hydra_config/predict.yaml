# @package _global_

hydra:
  # Do not modify the output paths. They are used for output path for netCDF, plotting, etc.
  run:
    dir: ${paths.predict}/${predict.seed}
  sweep:
    dir: ${paths.predict}
    subdir: ${predict.seed}
