add_era5_step:
    name: "Add ERA5 Data"
    command: preprocess_add_processed
    positional:
      - ${preprocess_params.config_name}
      - ${preprocess_era5.output_config_path}
    optional:
      --config-path: ${preprocess_params.config_file}
      -v: ${preprocess_params.verbose}
