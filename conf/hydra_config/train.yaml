# @package _global_

hydra:
  # Do not modify the output paths. They are used when loading checkpoint for prediction.
  run:
    dir: outputs/${train.run_name}/training/${train.seed}
  sweep:
    dir: outputs/${train.run_name}/training/
    subdir: ${train.seed}
